import { Host } from '../../../src/host';
import { expect } from 'chai';
import { stub } from 'sinon';

describe('#Host', () => {
    it('should ', () => {
        expect(1).to.be.equal(1);
    });
})
